// theme.ts — the ONE file that makes this codebase reusable for a new client. Change brand name / colors / feature flags here — never inside a component.
export const theme = {
  brandName: "Business Listing Theme (Demo)",
  colors: {
    primary: "#B11226",
    secondary: "#000000",
    accent: "#D4AF37",
  },
  features: {
    booking: true,
    gallery: true,
    reviews: true,
  },
};
